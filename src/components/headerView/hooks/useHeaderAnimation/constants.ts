// Distance (in px) before the header threshold over which the two headers crossfade.
export const CROSSFADE_DISTANCE = 40

// Floor for the crossfade threshold; guards against a zero-length interpolation
// range while the content height is still being measured.
export const MIN_THRESHOLD = 1
